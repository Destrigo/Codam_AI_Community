# Peer Review — Ollama Health Check

- [ ] `codamlings run ollama/01_check_version --lang python`
- [ ] Ollama running locally (`ollama serve`) or use `--mock` for offline verify
- [ ] No API keys in source — configure `OLLAMA_*` in repo root `.env`

## Approve
`codamlings review approve ollama/01_check_version --lang python --reviewer YOUR_NAME`
