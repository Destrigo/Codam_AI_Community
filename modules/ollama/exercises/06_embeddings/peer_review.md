# Peer Review — Ollama Embeddings

- [ ] `codamlings run ollama/06_embeddings --lang python`
- [ ] Ollama running locally (`ollama serve`) or use `--mock` for offline verify
- [ ] No API keys in source — configure `OLLAMA_*` in repo root `.env`

## Approve
`codamlings review approve ollama/06_embeddings --lang python --reviewer YOUR_NAME`
