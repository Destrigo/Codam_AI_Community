# Peer Review — PII Redaction

- [ ] `codam-labs run security/05_pii_redact --lang python`
- [ ] No API keys in source — use repo root `.env`

## Approve
`codam-labs review approve security/05_pii_redact --lang python --reviewer YOUR_NAME`

