# Peer Review — MCP Read Resource

- [ ] `codam-labs run mcp/04_resource_read --lang python`
- [ ] No API keys in source — use repo root `.env`

## Approve
`codam-labs review approve mcp/04_resource_read --lang python --reviewer YOUR_NAME`

