# Hint — 05_system_user_prompts

1. Reuse `chat_completion` from exercise 04 (copy the function if you no longer have it)
2. Pass **two** messages in the `messages` array
3. The system message goes before the user message
4. Do not uppercase the text yourself — the model must do it by following the system prompt
