# Hint — 06_conversation_history

1. The `messages` array must contain exactly 4 elements in the order specified
2. `assistant` messages in the history are **previous** responses — you write them as if they already happened
3. The mock counts `len(messages)` and responds `HISTORY_OK:<n>`
