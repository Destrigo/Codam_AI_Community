# Hint — 07_output_control

1. Add `max_tokens` to the JSON payload alongside `model` and `messages`
2. You do not need to set `temperature` for this exercise
3. The mock detects `max_tokens <= 5` and responds `TOKEN_LIMIT_OK`
